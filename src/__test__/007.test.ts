import { describe, expect, test } from "@jest/globals";

describe("Validação de Estruturas Diferentes com base em uma chave", () => {
  test("WIP", () => {
    expect(1 + 1).toBe(3); // Isso sempre será falso
  });
});